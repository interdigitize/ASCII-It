{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.17.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-materialize": "^1.1.2",
    "react-router-dom": "^4.2.2"
  },
  "scripts": {
    "start": "yarn start",
    "build": "yarn build",
    "test": "yarn test --env=jsdom",
    "eject": "yarn eject",
    "prettier-watch": "onchange '**/*.js' -- prettier --single-quote --flow --trailing-comma --write {{changed}}"
  },
  "devDependencies": {
    "onchange": "^3.3.0",
    "prettier": "^1.9.2"
  },
  "proxy": "http://localhost:3000"
}
